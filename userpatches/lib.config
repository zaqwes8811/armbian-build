# Add pciutils to the root filesystem
EXTRA_PACKAGES_ROOTFS="pciutils"

# Add the required references (one per package)
# This tells the build system where this package request came from
EXTRA_PACKAGES_ROOTFS_REFS="build:compile.sh:0"
